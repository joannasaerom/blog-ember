
import { postPreview } from 'blog/helpers/post-preview';
import { module, test } from 'qunit';

module('Unit | Helper | post preview');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postPreview([42]);
  assert.ok(result);
});

